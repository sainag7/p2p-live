# Copy to .env.local and set your keys. Never commit .env.local or any file containing real keys.

# Mapbox (required for map view). Used by Vite as VITE_MAPBOX_TOKEN in the client.
VITE_MAPBOX_TOKEN=your_mapbox_access_token_here

# Used only by the ops API server (server/index.js) for the Complaints LLM summary feature.
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: Gemini model (default: gemini-2.0-flash)
# GEMINI_MODEL=gemini-2.0-flash

# Optional: Ops API server port (default: 3001)
# OPS_API_PORT=3001
